{
  "name": "obsidian-rag-openrouter-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Obsidian plugin that retrieves note context and queries OpenRouter.",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "@types/node": "^20.16.10",
    "esbuild": "^0.24.0",
    "obsidian": "^1.6.7",
    "typescript": "^5.6.2"
  }
}